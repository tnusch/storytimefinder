__pycache__/
*.pyc
.venv/
venv/
env/
.env
*.egg-info/
.pytest_cache/

# Internal ops notes (setup/deploy/refresh how-to) - not for the public repo
INTERNAL.md

# Local SQLite DB (refresh environment only - never committed)
data/storytimefinder.db
data/storytimefinder.db-journal

# Claude-generated metadata suggestions for manual review (see refresh.py's
# generate_metadata_suggestions()) - a scratch staging file, not curated
# truth; approved values get copied into refresh/overrides.py by hand.
data/claude_suggestions.json

# Fetch-phase staging file (see refresh.py's --fetch/--sync split) - raw
# metadata for items not yet synced, plus each source's current item-id
# list for removal detection. Local/ephemeral, never curated truth.
data/fetched_items.json

# Individually-ignored admin-UI consistency/source warnings (see
# admin/file_ops.py's dismiss_warning()) - local review state, not
# curated truth, never pruned.
data/dismissed_warnings.json

# Vercel
.vercel

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/settings.json
.claude/settings.local.json
CLAUDE.md
.gitignore
